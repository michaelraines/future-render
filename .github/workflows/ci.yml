name: CI

on:
  push:
    branches: [main, "claude/**"]
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  # Primary CI job: lint, test, coverage, build on Linux
  ci:
    name: Lint, Test, Build (Linux)
    runs-on: ubuntu-latest

    strategy:
      matrix:
        go-version: ["1.24"]

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: ${{ matrix.go-version }}

      - name: Check formatting
        run: make fmt

      - name: Vet
        run: make vet

      - name: Install golangci-lint
        uses: golangci/golangci-lint-action@v9
        with:
          version: v2.5.0
          install-only: true

      - name: Lint
        run: make lint

      - name: Test
        run: make test

      - name: Coverage check (80% minimum)
        run: make cover-check

      - name: Test (race detector)
        run: make test-race

      - name: Build
        run: make build

  # Cross-platform build and test on macOS and Windows
  cross-platform:
    name: Test (${{ matrix.os }})
    runs-on: ${{ matrix.os }}

    strategy:
      fail-fast: false
      matrix:
        os: [macos-latest, windows-latest]
        go-version: ["1.24"]

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: ${{ matrix.go-version }}

      - name: Test
        run: make test

      - name: Build
        run: make build
